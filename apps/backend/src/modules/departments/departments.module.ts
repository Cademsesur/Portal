import { Module } from '@nestjs/common';

/**
 * Module DEPARTMENTS — gestion des départements + budgets.
 * Architecture clean : domain / application / infrastructure / interfaces.
 */
@Module({})
export class DepartmentsModule {}
