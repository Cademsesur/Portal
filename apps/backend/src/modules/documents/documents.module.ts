import { Module } from '@nestjs/common';

/**
 * Module DOCUMENTS — génération DOCX (docxtemplater) + PDF.
 * Job BullMQ déclenché par l'event request.approved.
 */
@Module({})
export class DocumentsModule {}
